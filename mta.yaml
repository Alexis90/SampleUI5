_schema-version: "2.0.0"
ID: "1.0.0"
version: 0.1.0

parameters:
  hcp-deployer-version: "1.0.0"

modules:
  - name: "SampleUI5App"
    type: html5
    path: .
    parameters:
       version: 1.0.0-${timestamp}
    build-parameters:
      builder: grunt
      build-result: dist
